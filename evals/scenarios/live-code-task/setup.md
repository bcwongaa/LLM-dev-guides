# Setup

Copy `evals/fixtures/scope-guard/` into an isolated clean git repository. Install the current
root instructions and expose the fixture commands documented in its README.

The baseline fixture is green before the task begins.
